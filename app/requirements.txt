# Core ORM + driver Postgres
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Supabase Auth (validação de JWT no backend + CLI)
supabase==2.10.0

# Environment
python-dotenv==1.0.1

# Web layer (api.py)
Flask==3.0.3
Flask-Cors==5.0.0
